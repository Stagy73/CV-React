/* import data from '../assets/data.js';

function Nuages() {
    const nuages = data.filter(item => item.hasOwnProperty('nuage', 'project')).map(item => `${item.nuage} ${item.project}`).join(', ');

    return (
        <div className="Nuages nuages-text">
            {nuages}
        </div>
    );
}

export default Nuages;
 */

function Nuages() {

    return (<div>
        <ul className="project">
            <li>Secondlife object ads clicker</li>
            <li>vente de livre libre de droit</li>
            <li>Dropishipping Amazon ebay</li>
            <li>website traffic antidetection project</li>
            <li>project de creation de dropshipping cdiscount</li>
            <li>gestion des clients</li></ul>
        <ul></ul>
    </div>)



}